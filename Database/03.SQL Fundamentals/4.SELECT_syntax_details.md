#### 1.1.Cấu trúc chuẩn của câu SELECT

Thứ tự **viết** khác thứ tự **thực thi** — đây là điểm hay bị hỏi phỏng vấn:

```sql
-- Thứ tự viết
SELECT col1, col2
FROM table_name
WHERE condition
GROUP BY COLUMN
HAVING group_condition
ORDER BY COLUMN
LIMIT n;

 -- Thứ tự thực thi (logical execution order)
FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
```

**Tại sao thứ tự quan trọng?** Vì bạn không thể dùng alias đặt trong `SELECT` ở mệnh đề `WHERE` (vì WHERE chạy trước SELECT), nhưng lại dùng được ở `ORDER BY` (vì ORDER BY chạy sau SELECT).

Ví dụ với bảng `products` (e-commerce):

```sql
CREATE TABLE products (
    id          SERIAL PRIMARY KEY,
    name        VARCHAR(255) NOT NULL,
    price       NUMERIC(10,2),
    stock_qty   INT,
    category_id INT,
    created_at  TIMESTAMP DEFAULT now()
);
```

```sql
-- Alias KHÔNG dùng được trong WHERE
SELECT price * 0.9 AS discounted_price
FROM products
WHERE discounted_price < 100;  -- ❌ Lỗi: column "discounted_price" does not exist

-- Nhưng dùng được trong ORDER BY
SELECT price * 0.9 AS discounted_price
FROM products
ORDER BY discounted_price;     -- ✅ OK
```

#### 1.2. WHERE — các toán tử lọc

```sql
-- So sánh cơ bản
SELECT * FROM products WHERE price > 50;

-- IN: kiểm tra trong tập giá trị
SELECT * FROM products WHERE category_id IN (1, 2, 5);

-- BETWEEN: khoảng giá trị (bao gồm cả 2 đầu)
SELECT * FROM products WHERE price BETWEEN 100 AND 500;

-- LIKE: khớp mẫu chuỗi
-- % : bất kỳ chuỗi ký tự nào (kể cả rỗng)
-- _ : đúng 1 ký tự
SELECT * FROM products WHERE name LIKE 'iPhone%';   -- bắt đầu bằng "iPhone"
SELECT * FROM products WHERE name LIKE '%Pro%';     -- chứa "Pro"
SELECT * FROM products WHERE name ILIKE '%pro%';    -- không phân biệt hoa/thường (Postgres riêng)

-- IS NULL / IS NOT NULL
-- Lưu ý: KHÔNG dùng "= NULL" vì NULL không so sánh bằng được với bất kỳ giá trị nào (kể cả NULL)
SELECT * FROM products WHERE category_id IS NULL;
```

⚠️ **Bẫy phỏng vấn hay gặp:** `NOT IN` với danh sách chứa `NULL` sẽ trả về **rỗng** (không có dòng nào), vì so sánh với NULL luôn ra `UNKNOWN`. Đây là lỗi rất phổ biến khi dev viết subquery.

```sql
-- Nếu category_id có giá trị NULL trong bảng con, kết quả sẽ SAI (trả về rỗng)
SELECT * FROM products
WHERE category_id NOT IN (SELECT category_id FROM categories WHERE is_active = true );

-- Cách fix an toàn: dùng NOT EXISTS thay vì NOT IN
SELECT * FROM products p 
WHERE NOT EXISTS (
    SELECT 1 FROM categories c
    WHERE c.id = p.category_id AND c.is_active = false
); 
```

#### **1.3. DISTINCT — loại bỏ trùng lặp**

```sql
-- Lấy danh sách tên sản phẩm duy nhất    
SELECT DISTINCT product_id FROM products;

-- DISTINCT áp dụng toàn bộ tổng hợp cột được chọn, không phải từng cột
SELECT DISTINCT product_name, price FROM products; 
```

#### **1.4. ORDER BY**

```sql
SELECT product_name, price FROM products
ORDER BY price DESC, name ASC; -- sắp theo price giảm dần, nếu trùng thì name tăng dần

-- Sắp theo vị trí cột (không khuyến khích vì khó đọc/maintain)
SELECT name, price FROM products ORDER BY 2 DESC;

-- NULL trong ORDER BY: mặc định NULL được coi là LỚN NHẤT khi ASC (đứng cuối)
-- Postgres cho phép chỉnh:
SELECT * FROM products ORDER BY category_id NULLS LAST;
SELECT * FROM products ORDER BY category_id NULLS FIRST;
```

#### **1.5. LIMIT / OFFSET — phân trang**

```sql
-- Lấy 10 sản phẩm, bỏ qua 20 sản phẩm đầu (trang 3, page size = 10)
SELECT * FROM products
ORDER BY id
LIMIT 10 OFFSET 20; 
```

⚠️ **Vấn đề hiệu năng thực tế:** `OFFSET` lớn sẽ chậm dần vì DB vẫn phải quét qua các dòng bị bỏ. Với bảng lớn, dùng **keyset pagination** (cursor-based) sẽ nhanh hơn nhiều:

```sql
-- Thay vì OFFSET, dùng điều kiện dựa trên id/created_at của dòng cuối trang trước
SELECT * FROM products
WHERE id > 12345  -- id cuối cùng của trang trước
ORDER BY id
LIMIT 10; 
```

#### 1.6. Ví dụ tổng hợp toàn bộ mệnh đề

```sql
SELECT id, name, price
FROM products
WHERE price BETWEEN 100 AND 1000
  AND stock_qty > 0
  AND name ILIKE '%laptop%'
ORDER BY price DESC
LIMIT 5;
```

----

**Q: Thứ tự thực thi thực sự (logical order) của một câu SELECT là gì?** Vì sao alias trong SELECT không dùng được trong WHERE nhưng dùng được trong ORDER BY?

> Thứ tự thực thi: `FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT`. 
> 
> Alias được tạo ra ở bước `SELECT`. Vì `WHERE`  chạy **trước** `SELECT` nên tại thời điểm đó alias chưa tồn tại → lỗi. Còn `ORDER BY` chạy **sau** `SELECT` nên alias đã có sẵn, dùng được bình thường.



**Q: Sự khác nhau giữa `WHERE` và `HAVING`?** (câu này liên quan phần sau, nhưng hay hỏi chung với SELECT)

> `WHERE` lọc từng dòng dữ liệu thô, chạy **trước khi** nhóm (GROUP BY). `HAVING` lọc trên kết quả đã nhóm/tổng hợp, chạy **sau khi** GROUP BY. Vì vậy `WHERE` không dùng được với hàm tổng hợp (`SUM`, `COUNT`...), còn `HAVING` thì dùng được.



**Q: Tại sao `column = NULL` luôn sai, phải dùng `IS NULL`?** Giải thích three-valued logic (TRUE/FALSE/UNKNOWN) trong SQL.

> SQL dùng logic 3 giá trị: `TRUE`, `FALSE`, `UNKNOWN`. NULL nghĩa là "giá trị không xác định", nên so sánh bất kỳ thứ gì với NULL (kể cả NULL với NULL) đều trả về `UNKNOWN`, không phải `TRUE` — dòng đó sẽ bị loại khỏi kết quả `WHERE`. Phải dùng `IS NULL` / `IS NOT NULL` vì đây là toán tử kiểm tra riêng, không phải phép so sánh giá trị.



**Q: `NOT IN` với subquery chứa NULL có vấn đề gì?** Cách fix bằng `NOT EXISTS`.

> Nếu subquery trả về dù chỉ 1 giá trị NULL, toàn bộ `NOT IN` sẽ trả về rỗng, vì về bản chất `NOT IN (a, b, NULL)` tương đương `x != a AND x != b AND x != NULL` — mà `x != NULL` luôn là `UNKNOWN`, làm cả biểu thức AND thành `UNKNOWN`/không match. Cách fix an toàn là dùng `NOT EXISTS`, vì nó không so sánh giá trị trực tiếp mà kiểm tra sự tồn tại của dòng khớp.



**Q: `OFFSET/LIMIT` có nhược điểm gì khi phân trang dữ liệu lớn?** Đề xuất giải pháp thay thế (keyset/cursor pagination) — cách triển khai ra sao.

> DB vẫn phải **quét và đếm qua** toàn bộ số dòng bị OFFSET trước khi lấy dữ liệu thực sự, dù không trả về chúng. Với OFFSET = 1 triệu, DB vẫn quét 1 triệu dòng → càng về sau càng chậm. Giải pháp: **keyset/cursor pagination** — dùng điều kiện `WHERE id > last_seen_id ORDER BY id LIMIT n`, tận dụng được index, tốc độ ổn định bất kể trang nào.



**Q: `DISTINCT` hoạt động trên nhiều cột như thế nào?**

>  `DISTINCT` áp dụng cho **toàn bộ tổ hợp** các cột được chọn, không phải riêng từng cột. `SELECT DISTINCT a, b` sẽ loại bỏ các dòng có **cả cặp (a,b) giống hệt nhau**, chứ không loại trùng riêng từng cột a hay b.



**Q: **Cho một bảng `orders(id, customer_id, total, created_at)`, viết query lấy **5 đơn hàng gần nhất** của khách hàng có `customer_id = 10`.

```sql

```
