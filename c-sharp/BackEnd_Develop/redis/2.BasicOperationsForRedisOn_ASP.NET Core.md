 Dưới đây là danh sách các **thao tác phổ biến trên Redis** mà bạn có thể thực hiện thông qua **ASP.NET Core** bằng cách sử dụng thư viện **StackExchange.Redis**. Các thao tác này bao gồm từ cơ bản đến nâng cao

---

### **1. Các thao tác cơ bản**
#### **a. Lưu và đọc dữ liệu dạng string**
- **Lưu dữ liệu**:
```
public async Task SetStringAsync(string key, string value)
{
    var db = _connection.GetDatabase();
    await db.StringSetAsync(key, value);
}
```
- **Đọc dữ liệu**:
```
```