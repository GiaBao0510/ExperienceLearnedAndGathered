Trước khi bắt đầu trải nghiệm GitHub Actions, hãy cùng tìm hiểu xem chúng là gì và hoạt động như thế nào.

---
## **Introduction to some terms**
### [Workflows](https://docs.github.com/en/actions/get-started/understand-github-actions#workflows)
Workflow là một quy trình tự động có thể tùy chỉnh, dùng để thực thi một hoặc nhiều tác vụ (job). Các workflow được định nghĩa bằng một tệp YAML lưu trong kho mã nguồn (repository) và sẽ chạy khi được kích hoạt bởi một sự kiện trong kho mã nguồn, hoặc có thể được kích hoạt thủ công hay theo lịch trình đã định.

Các quy trình làm việc (workflow) được định nghĩa trong thư mục `.github/workflows` của một kho lưu trữ (repository). Một kho lưu trữ có thể chứa nhiều quy trình làm việc, trong đó mỗi quy trình có thể thực hiện các nhóm tác vụ khác nhau, chẳng hạn như:

- Build và kiểm thử các pull request
- Triển khai ứng dụng mỗi khi một bản phát hành (release) được tạo
- Thêm nhãn (label) bất cứ khi nào có một issue mới được mở

Bạn có thể tham chiếu một quy trình làm việc này từ một quy trình làm việc khác. Để biết thêm thông tin, hãy xem phần Tái sử dụng quy trình làm việc [Reuse workflows](https://docs.github.com/en/actions/how-tos/reuse-automations/reuse-workflows).

Để biết thêm thông tin, hãy xem phần Viết quy trình làm việc [Writing workflows](https://docs.github.com/en/actions/how-tos/write-workflows).

### [Events](https://docs.github.com/en/actions/get-started/understand-github-actions#events)

Sự kiện là một hoạt động cụ thể trong kho lưu trữ (repository) giúp kích hoạt quá trình chạy quy trình làm việc (workflow). Ví dụ: một hoạt động có thể bắt nguồn từ GitHub khi ai đó tạo pull request, mở issue hoặc đẩy (push) một commit vào kho lưu trữ. Bạn cũng có thể kích hoạt quy trình làm việc theo [lịch trình](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule), thông qua việc gửi yêu cầu đến [REST API hoặc kích hoạt thủ công](https://docs.github.com/en/rest/repos/repos#create-a-repository-dispatch-event).

Để xem danh sách đầy đủ các sự kiện có thể dùng để kích hoạt quy trình làm việc, hãy tham khảo mục ["Các sự kiện kích hoạt quy trình làm việc"](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows).

### [Jobs](https://docs.github.com/en/actions/get-started/understand-github-actions#jobs)

Một job (công việc) bao gồm một chuỗi các bước trong quy trình làm việc (workflow) được thực thi trên cùng một runner. Mỗi bước có thể là một shell script hoặc một action. Các bước được thực thi theo thứ tự và có sự phụ thuộc lẫn nhau. Do tất cả các bước đều chạy trên cùng một runner, bạn có thể chia sẻ dữ liệu giữa các bước. Ví dụ: bạn có thể thiết lập một bước để build ứng dụng, theo sau là một bước để kiểm thử ứng dụng vừa được build đó.

Theo mặc định, các bước sẽ chạy tuần tự, nhưng bạn cũng có thể cho phép một số bước nhất định chạy đồng thời nếu quy trình làm việc của bạn đạt hiệu quả cao hơn nhờ cơ chế song song—chẳng hạn như khởi động một dịch vụ chạy trong thời gian dài trong khi các bước khác vẫn tiếp tục thực hiện. Để biết thêm thông tin, hãy xem phần [Cú pháp workflow cho GitHub Actions](https://docs.github.com/en/actions/get-started/understand-github-actions#jobs:~:text=Workflow%20syntax%20for%20GitHub%20Actions.).

Bạn có thể cấu hình mối quan hệ phụ thuộc giữa các job; theo mặc định, các job không phụ thuộc lẫn nhau và sẽ chạy song song. Khi một job phụ thuộc vào một job khác, nó sẽ chờ job đó hoàn tất rồi mới bắt đầu chạy.

Bạn cũng có thể sử dụng ma trận (matrix) để chạy cùng một job nhiều lần, với mỗi lần chạy sử dụng một tổ hợp biến khác nhau—ví dụ như các hệ điều hành hoặc phiên bản ngôn ngữ khác nhau.

Chẳng hạn, bạn có thể cấu hình nhiều job build cho các kiến ​​trúc khác nhau mà không thiết lập sự phụ thuộc giữa chúng, đồng thời thiết lập một job đóng gói (packaging) phụ thuộc vào các job build đó. Các job build sẽ chạy song song, và khi chúng hoàn tất thành công, job đóng gói mới bắt đầu chạy.

Để biết thêm thông tin, [hãy xem phần Lựa chọn các tác vụ cho workflow](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do).

---
GitHub Actions hoạt động dựa trên các quy trình làm việc (workflow). Một quy trình làm việc bao gồm một chuỗi các tác vụ (job) được thực thi khi có một sự kiện cụ thể kích hoạt chúng. Mỗi tác vụ chứa các chỉ dẫn riêng để điều phối hoạt động của GitHub Actions.

Quá trình thực thi một quy trình làm việc điển hình diễn ra như sau:

- Sự kiện kích hoạt xảy ra (ví dụ: có thao tác đẩy mã nguồn lên nhánh chính).
- Quy trình làm việc tương ứng với sự kiện đó được thực thi.
- Dọn dẹp (cleanup).

---
### Basic needs

Nhìn chung, để CI hoạt động trên một kho mã nguồn (repository), cần có một số yếu tố sau:

- Một kho mã nguồn (điều hiển nhiên)
- Các định nghĩa về những việc CI cần thực hiện: Thông tin này có thể nằm trong một tệp cụ thể bên trong kho mã nguồn hoặc được cấu hình trực tiếp trên hệ thống CI
- Hệ thống CI cần nhận biết được sự tồn tại của kho mã nguồn (và tệp cấu hình bên trong đó)
- Hệ thống CI cần có khả năng truy cập vào kho mã nguồn
- Hệ thống CI cần có quyền thực hiện các tác vụ được giao: Ví dụ, nếu CI cần triển khai ứng dụng lên môi trường sản xuất (production), nó cần có thông tin xác thực để truy cập môi trường đó.

Đó là mô hình truyền thống; lát nữa chúng ta sẽ thấy cách GitHub Actions giúp bỏ qua một số bước này – hay nói đúng hơn là giúp bạn không còn phải bận tâm đến chúng nữa!

GitHub Actions có một ưu điểm lớn so với các giải pháp tự lưu trữ (self-hosted): kho mã nguồn (repository) được lưu trữ ngay tại nơi cung cấp dịch vụ CI. Nói cách khác, GitHub cung cấp cả kho mã nguồn lẫn nền tảng CI. Điều này có nghĩa là khi chúng ta kích hoạt Actions cho một kho mã nguồn, GitHub đã nắm được thông tin về các quy trình làm việc (workflow) mà chúng ta đã thiết lập cũng như nội dung chi tiết của chúng.

---
#### **Exercise: The example project**

**Hướng dẫn**
Trong hầu hết các bài tập của phần này, chúng ta sẽ xây dựng một quy trình CI/CD cho một dự án nhỏ có sẵn trong [kho mã nguồn mẫu này]([fullstack-hy2020/fs-pokedex](https://github.com/fullstack-hy2020/fs-pokedex)) 

Như thường lệ, khi bắt đầu với mã nguồn mới, nơi đầu tiên cần xem xét là tệp `package.json`.

Bây giờ, hãy thử thực hiện các bước sau:
1. cài đặt các phụ thuộc (dependencies)
```powershell
PS E:\DoAn\fs-pokedex> npm install     

up to date, audited 1016 packages in 26s

246 packages are looking for funding
  run `npm fund` for details

29 vulnerabilities (2 low, 9 moderate, 16 high, 2 critical)

To address all issues, run:
  npm audit fix

Run `npm audit` for details.
```


2. khởi chạy mã nguồn ở chế độ phát triển (development mode)
```powershell
PS E:\DoAn\fs-pokedex> npm start       

> fullstackopen-cicd@1.0.0 start
> webpack-dev-server --open --mode development --port 8080

<i> [webpack-dev-server] Project is running at:
<i> [webpack-dev-server] Loopback: http://localhost:8080/, http://[::1]:8080/
<i> [webpack-dev-server] On Your Network (IPv4): http://192.168.0.106:8080/
<i> [webpack-dev-server] Content not from webpack is served from 'E:\DoAn\fs-pokedex\public' directory
<i> [webpack-dev-server] 404s will fallback to '/index.html'
<i> [webpack-dev-middleware] wait until bundle finished: /
asset bundle.js 1.9 MiB [emitted] (name: main)
asset ./index.html 269 bytes [emitted]
runtime modules 28.3 KiB 15 modules
orphan modules 134 KiB [orphan] 7 modules
modules by path ./node_modules/ 1.66 MiB 80 modules
modules by path ./src/ 15.7 KiB
  modules by path ./src/*.jsx 7.14 KiB
```

3. chạy các bài kiểm thử (tests)
```powershell
PS E:\DoAn\fs-pokedex> npm test        

> fullstackopen-cicd@1.0.0 test
> jest

 PASS  test/PokemonList.jest.spec.jsx (21.877 s)
 PASS  test/App.jest.spec.jsx (22.131 s)
 FAIL  test/PokemonPage.jest.spec.jsx (22.345 s)
  ● Console

    console.log
      hiddenAbility= {
        ability: {
          name: 'anticipation',
          url: 'https://pokeapi.co/api/v2/ability/107/'
        },
        is_hidden: true,
        slot: 3
      }

      at log (src/PokemonPage.jsx:29:11)

    console.log
      hiddenAbility= {
        ability: {
          name: 'anticipation',
          url: 'https://pokeapi.co/api/v2/ability/107/'
        },
        is_hidden: true,
        slot: 3
      }

      at log (src/PokemonPage.jsx:29:11)

    console.log
      hiddenAbility= {
        ability: {
          name: 'anticipation',
          url: 'https://pokeapi.co/api/v2/ability/107/'
        },
        is_hidden: true,
        slot: 3
      }

      at log (src/PokemonPage.jsx:29:11)

    console.log
      hiddenAbility= {
        ability: {
          name: 'anticipation',
          url: 'https://pokeapi.co/api/v2/ability/107/'
        },
        is_hidden: true,
        slot: 3
      }

      at log (src/PokemonPage.jsx:29:11)

  ● <PokemonPage /> › should render previous and next urls if they exist

    expect(element).toHaveAttribute("href", "/pokemon/vaporeon") // element.getAttribute("href") === "/pokemon/vaporeon"

    Expected the element to have attribute:
      href="/pokemon/vaporeon"
    Received:
      href="/pokemon/ditto"

      114 |
      115 |     expect(screen.getByText('Previous')).toHaveAttribute('href', '/pokemon/ditto')
    > 116 |     expect(screen.getByText('Next')).toHaveAttribute('href', '/pokemon/vaporeon')
          |                                      ^
      117 |   })
      118 |
      119 |   it('should not render previous and next urls if none exist', async () => {

      at toHaveAttribute (test/PokemonPage.jest.spec.jsx:116:38)
      at node_modules/@babel/runtime/helpers/regeneratorRuntime.js:52:18
      at Generator.<anonymous> (node_modules/@babel/runtime/helpers/regenerator.js:52:51)
      at Generator.next (node_modules/@babel/runtime/helpers/regeneratorDefine.js:11:21)
      at asyncGeneratorStep (node_modules/@babel/runtime/helpers/asyncToGenerator.js:3:17)
      at _next (node_modules/@babel/runtime/helpers/asyncToGenerator.js:17:9)

Test Suites: 1 failed, 2 passed, 3 total
Tests:       1 failed, 6 passed, 7 total
Snapshots:   0 total
Time:        51.649 s
Ran all test suites.
```

4. kiểm tra lỗi cú pháp và định dạng mã nguồn (linting)
```powershell
PS E:\DoAn\fs-pokedex> npm run eslint  

> fullstackopen-cicd@1.0.0 eslint
> eslint './**/*.{js,jsx}'


Oops! Something went wrong! :(

ESLint: 9.39.4

No files matching the pattern "'./**/*.{js,jsx}'" were found.
Please check for typing mistakes in the pattern.
```

Bạn có thể nhận thấy dự án hiện có một số bài kiểm thử (test) bị lỗi và các lỗi linting. Hãy cứ để nguyên như vậy vào lúc này; chúng ta sẽ xử lý chúng sau trong các bài tập.

LƯU Ý: các bài kiểm thử của dự án được viết bằng [Jest](https://jestjs.io/). Tài liệu khóa học ở [phần 5](https://fullstackopen.com/en/part5/testing_react_apps) lại sử dụng [Vitest](https://vitest.dev/guide/). Xét về cách sử dụng, hai thư viện này hầu như không có sự khác biệt nào.

Như bạn có thể còn nhớ từ [phần 3](https://fullstackopen.com/en/part3/deploying_app_to_internet#frontend-production-build), mã nguồn React không nên chạy ở chế độ phát triển (development mode) sau khi đã được triển khai lên môi trường thực tế (production). Bây giờ, hãy thử thực hiện các bước sau:

- tạo bản build production cho dự án
- chạy phiên bản production trên máy cục bộ

Đối với hai tác vụ này, dự án cũng đã có sẵn các tập lệnh npm (npm scripts) để sử dụng!

Hãy dành chút thời gian tìm hiểu cấu trúc của dự án. Bạn sẽ thấy cả mã nguồn frontend và backend hiện đều nằm trong [cùng một kho lưu trữ (repository)](https://fullstackopen.com/en/part7/class_components_miscellaneous#frontend-and-backend-in-the-same-repository). Ở các phần trước của khóa học, chúng ta sử dụng các kho lưu trữ riêng biệt cho từng phần, nhưng việc gộp chung vào một kho lưu trữ sẽ giúp đơn giản hóa rất nhiều quá trình thiết lập môi trường CI.

Khác với hầu hết các dự án trong khóa học này, mã nguồn frontend không sử dụng Vite mà dùng cấu hình [Webpack](https://fullstackopen.com/en/part7/webpack) tương đối đơn giản để xử lý việc tạo môi trường phát triển cũng như tạo gói (bundle) cho môi trường production.

---
## Getting started with workflows

Thành phần cốt lõi để tạo các pipeline CI/CD với GitHub Actions là thứ được gọi là Workflow (quy trình làm việc). Workflow là các luồng quy trình mà bạn có thể thiết lập trong kho lưu trữ (repository) của mình để thực hiện các tác vụ tự động như build (xây dựng), test (kiểm thử), lint (kiểm tra lỗi cú pháp/phong cách code), release (phát hành) và deploy (triển khai), cùng nhiều tác vụ khác! Cấu trúc phân cấp của một workflow như sau:

Workflow
- Job (Công việc)
	- Step (Bước)
	- Step (Bước)
- Job (Công việc)
	- Step (Bước)

Mỗi workflow phải chỉ định ít nhất một [Job](https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/introduction-to-github-actions#jobs); job này chứa một tập hợp các [Step](https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/introduction-to-github-actions#steps) để thực hiện các tác vụ riêng lẻ. Các job sẽ chạy song song, trong khi các step bên trong mỗi job sẽ được thực thi tuần tự.

Các step có thể đa dạng, từ việc chạy một lệnh tùy chỉnh cho đến sử dụng các action (hành động) được định nghĩa sẵn – đó cũng chính là lý do nó có tên là GitHub Actions. Bạn có thể tạo các [action tùy chỉnh](https://docs.github.com/en/free-pro-team@latest/actions/creating-actions) hoặc sử dụng bất kỳ action nào do cộng đồng chia sẻ (có rất nhiều action như vậy), nhưng chúng ta sẽ nói về điều này sau nhé!

Để GitHub nhận diện được các workflow của bạn, chúng cần được đặt trong thư mục `.github/workflows` của kho lưu trữ. Mỗi Workflow là một tệp riêng biệt và cần được cấu hình bằng ngôn ngữ [YAML](https://yaml.org/).

YAML là một từ viết tắt đệ quy có nghĩa là "YAML Ain't Markup Language" (YAML không phải là ngôn ngữ đánh dấu). Đúng như tên gọi, mục tiêu của nó là dễ đọc đối với con người và thường được sử dụng cho các tệp cấu hình.

Lưu ý rằng việc thụt lề (indentation) rất quan trọng trong YAML. Bạn có thể tìm hiểu thêm về cú pháp [tại đây](https://docs.ansible.com/ansible/latest/reference_appendices/YAMLSyntax.html).

Một workflow cơ bản bao gồm ba thành phần trong tài liệu YAML. Ba thành phần đó là:

- `name`: đây là tên của workflow.
- `(on) triggers`: Các sự kiện kích hoạt việc thực thi workflow.
- `jobs`: Các công việc riêng biệt mà workflow sẽ thực hiện (một workflow cơ bản có thể chỉ chứa một job).

Dưới đây là ví dụ về định nghĩa một workflow đơn giản:

```yaml
name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
```

Có một job tên là `hello_world_job`, sẽ được thực thi trong môi trường ảo chạy Ubuntu 24.04. Job này chỉ bao gồm một bước duy nhất mang tên "Say hello", thực hiện lệnh `echo "Hello World!"` trong shell.

Vậy bạn có thể thắc mắc: khi nào thì GitHub kích hoạt một workflow? Có rất nhiều tùy chọn để bạn [lựa chọn](https://docs.github.com/en/free-pro-team@latest/actions/reference/events-that-trigger-workflows), nhưng nhìn chung, bạn có thể cấu hình để workflow bắt đầu khi:

- Một sự kiện trên GitHub xảy ra, chẳng hạn như khi ai đó đẩy (push) một commit lên kho lưu trữ (repository), hoặc khi một issue hay pull request được tạo ra.
- Một sự kiện theo lịch trình (được xác định bằng cú pháp [cron](https://en.wikipedia.org/wiki/Cron)) diễn ra.
- Một sự kiện bên ngoài xảy ra; ví dụ: một lệnh được thực hiện trong ứng dụng bên ngoài như Slack hoặc Discord.

Để tìm hiểu thêm về các sự kiện có thể dùng để kích hoạt workflow, vui lòng tham khảo [tài liệu của GitHub Actions](https://docs.github.com/en/free-pro-team@latest/actions/reference/events-that-trigger-workflows).

---
### **Exercise: Hello world!**

Hướng dẫn
Hãy tạo một Workflow mới để xuất dòng chữ "Hello World!" ra bảng điều khiển (console). Để thiết lập, bạn cần tạo thư mục `.github/workflows` và tệp `hello.yml` trong kho lưu trữ (repository) của mình.

Để xem kết quả thực thi của workflow GitHub Action, bạn có thể truy cập vào tab **Actions** trên GitHub; tại đây, bạn sẽ thấy các workflow có trong kho lưu trữ cùng các bước mà chúng thực hiện. Nếu workflow được cấu hình đúng cách, kết quả đầu ra của workflow "Hello World" sẽ có dạng như sau:

![](https://github.com/GiaBao0510/ExperienceLearnedAndGathered/blob/main/DevOps/CICD_Guide/img/GithubAction_Hello_world!.png)

Bạn sẽ thấy thông báo "Hello World!" xuất hiện trong phần kết quả đầu ra. Nếu vậy, nghĩa là bạn đã hoàn tất thành công mọi bước cần thiết. Quy trình làm việc (workflow) GitHub Actions đầu tiên của bạn đã được kích hoạt!

Lưu ý rằng GitHub Actions cũng cung cấp thông tin chi tiết về môi trường (hệ điều hành và cấu hình đi kèm) nơi quy trình của bạn được thực thi. Điều này rất quan trọng, bởi nếu xảy ra sự cố bất ngờ, việc có thể tái hiện lại toàn bộ các bước trên máy tính cá nhân sẽ giúp công việc gỡ lỗi trở nên dễ dàng hơn nhiều!

---
### **Exercise:Date and directory contents**

**Hướng dẫn**
Mở rộng quy trình làm việc bằng cách thêm các bước hiển thị ngày tháng và nội dung thư mục hiện tại ở định dạng chi tiết (long format).

Đây đều là những bước đơn giản; bạn chỉ cần chạy các lệnh `date` và `ls` là được.

```yml
name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: "Run date"
        run: |
          date
      - name: "Run ls -l"
        run: |
          ls -l
```

Quy trình làm việc của bạn lúc này sẽ có dạng như sau:
![](https://github.com/GiaBao0510/ExperienceLearnedAndGathered/blob/main/DevOps/CICD_Guide/img/GithubAction_DateAndDirectoryContents.png)

Như kết quả của lệnh `ls -l` cho thấy, theo mặc định, môi trường ảo thực thi quy trình làm việc của chúng ta không chứa bất kỳ mã nguồn nào!

---
Phần hướng dẫn [tiếp theo](obsidian://open?vault=CuuAmChanKinh&file=DevOps%2FCICD_Guide%2F2.2%20Getting%20started%20with%20GitHub%20Actions)
